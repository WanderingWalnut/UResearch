# Approved design reference

Start with [DESIGN.md](../../DESIGN.md). It defines the accepted iteration 04 design system, reusable components, motion, and review rules.

[Open the frozen screens](iteration-04/index.html) through a local HTTP server (for example, `python3 -m http.server 8767 --directory docs/design`). The HTML was copied from review-site commit `f5c70768ec92fcbf7007ae2ce7d7894294fb0b51`, approved on 5 September 2026. Preserve this snapshot when implementation changes. Its CSS includes older overridden rules; use the production tokens and components instead of copying those styles.

The separate logo study is omitted: **UResearch.** is the approved text wordmark. Data and actions in the frozen pages are fictional. External fonts load from Google Fonts. Reference pages are not imported by or shipped as routes in the application.
